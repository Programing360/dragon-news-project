import React from 'react';
import { Outlet } from 'react-router-dom';

const AuthRegister = () => {
    return (
        <div>
            <Outlet></Outlet>
        </div>
    );
};

export default AuthRegister;